if(MAKE_LITE)

add_gmac_library(gmac-core-lite STATIC ${gmac-core-lite_SRC})
target_link_libraries(gmac-core-lite gmac-core gmac-memory gmac-common gmac-trace)

if(MAKE_STATIC)
    add_gmac_library(gmac-lite STATIC ${gmac-lite_SRC} ${gmac-include_SRC})
else(MAKE_STATIC)
    add_gmac_library(gmac-lite SHARED ${gmac-lite_SRC} ${gmac-include_SRC})
endif(MAKE_STATIC)
target_link_libraries(gmac-lite gmac-common gmac-trace gmac-core gmac-core-lite gmac-memory gmac-core-lite gmac-libs ${gmac_LIBS})
set_target_properties(gmac-lite PROPERTIES
    VERSION ${GMAC_VERSION} SOVERSION 1
)

set(GMAC_HEADERS ${GMAC_HEADERS} 
                 ${CMAKE_SOURCE_DIR}/src/include/gmac/lite
                 ${CMAKE_SOURCE_DIR}/src/include/gmac/lite.h
                 ${CMAKE_SOURCE_DIR}/src/include/gmac/lite_types.h)


set(GMAC_TARGET_NAME ${GMAC_TARGET_NAME} gmac-lite)

#install(TARGETS gmac-lite 
#     LIBRARY DESTINATION lib
#     ARCHIVE DESTINATION lib
#     RUNTIME DESTINATION lib
#     RESOURCE DESTINATION share
#     PUBLIC_HEADER DESTINATION include/gmac)

set(gmac-core-lite_LIB gmac-core-lite)

endif(MAKE_LITE)

